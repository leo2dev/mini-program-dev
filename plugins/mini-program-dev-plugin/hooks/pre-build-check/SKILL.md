# pre-build-check

## 功能

构建前检查，确保代码符合规范，避免常见问题。

## 检查项

1. **页面检查**: pages.json 是否完整
2. **组件检查**: 组件是否正确注册
3. **API 检查**: 接口地址是否正确配置

## 触发时机

- `build-deploy` skill 执行前自动触发
- 可通过设置 `settings.json` 启用/禁用

## 输出

检查结果以注释形式输出，无阻塞性错误时继续执行。
